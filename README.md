# Send To Datalayer Tag Template (Client-side)

A simple Google Tag Manager tag template to manage dataLayer.push() event from inside Google Tag Manager. Includes input fields for raw event name, plus add easily event paramaters ad nauseum, using all kinds of variables available inside GTM.
